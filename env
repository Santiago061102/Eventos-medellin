# .env — Variables de entorno
# Copia este archivo como .env y rellena con tus datos reales
# NUNCA subas el .env a GitHub

# ── Base de datos ────────────────────────────────────────────
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=tu_password_aqui
DB_NAME=medellin_eventos

# ── Servidor ─────────────────────────────────────────────────
PORT=3000

# ── Railway (se llenan automáticamente en Railway) ───────────
# MYSQL_URL=mysql://user:pass@host:port/dbname
# DATABASE_URL=mysql://user:pass@host:port/dbname
